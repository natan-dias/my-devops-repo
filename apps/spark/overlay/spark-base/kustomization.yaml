namespace: spark
resources:
  - ../../base
  - service.yaml
  - ingress.yaml
  - spark.yaml
  - spark-worker.yaml
  - storage.yaml

secretGenerator:
  - name: secret-spark.local
    files:
      - tls.key=<spark-base:spark-local.key>
      - tls.crt=<spark-base:spark-local.crt>
    type: "kubernetes.io/tls"
