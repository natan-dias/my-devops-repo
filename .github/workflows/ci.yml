name: CI to Docker Hub

on:
  workflow_dispatch:
    branches:
      - main
    tags: [ 'v*' ]
    inputs:
      folder: 
        description: 'Dockerfile folder location'
        required: true
        type: string
        default: './images/'
      release_tag:
        description: 'Release tag'
        required: true
        type: string
        default: 'latest'

jobs:
  build-docker-image-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    outputs:
      last_release_tag: ${{ steps.get_last_release_tag.outputs.LAST_RELEASE_TAG }}
    steps:
    - name: Checkout repo
      uses: actions/checkout@v3.5.2
    
    - name: Log in to Docker Hub
      uses: docker/login-action@v2.1.0
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Set Repo Name
      run: |
        echo "REPO=natandias1" >> $GITHUB_ENV
        echo "Repo name: ${{ env.REPO }}"
    
    - name: "Set envs"
      id: set-envs
      run: | 
        IMAGE_NAME_FOLDER=$(awk -F "/" '{print $NF}' <<< ${{ github.event.inputs.folder }})
        echo "IMAGE_NAME_FOLDER=$IMAGE_NAME_FOLDER" >> $GITHUB_ENV
        echo "Image name folder: $IMAGE_NAME_FOLDER"
    
    - name: Get Last release tag
      uses: actions/github-script@v3
      id: get_last_release_tag
      with:
        script: |
          const { data: releases } = await github.repos.listReleases({
            owner: context.repo.owner,
            repo: context.repo.repo
          });

          const lastReleaseTag = releases[0].tag_name;
          core.setOutput('last_release_tag', lastReleaseTag);

          console.log(`Last release tag: ${lastReleaseTag}`);
        run: |
          echo "LAST_RELEASE_TAG=${lastReleaseTag}" >> $GITHUB_OUTPUT


    - name: Build and Push Docker Image
      id: build-and-push
      run: |
        docker build -t ${{ env.REPO }}/${{ env.IMAGE_NAME_FOLDER }}:${{ needs.build-docker-image-and-push.outputs.last_release_tag }} -f ${{ github.event.inputs.folder }}/Dockerfile .
        docker push ${{ env.REPO }}/${{ env.IMAGE_NAME_FOLDER }}:${{ needs.build-docker-image-and-push.outputs.last_release_tag }}
    
    #- name: Docker Metadata action
    #  id: meta
    #  uses: docker/metadata-action@v4.4.0
    #  with:
    #    images: ${{ env.REPO }}/${{ env.IMAGE_NAME_FOLDER }}
    #    tags: |
    #      type=ref,event=branch
    #      type=ref,event=tag
    #      type=semver,pattern={{major}}.{{minor}}.{{patch}}
    #  
    #- name: Build and push Docker images
    #  uses: docker/build-push-action@v4.0.0
    #  with:
    #    file: ${{ github.event.inputs.folder }}/Dockerfile
    #    context: .
    #    tags: ${{ steps.meta.outputs.tags }}
    #    push: ${{ github.event_name != 'pull_request' }}
